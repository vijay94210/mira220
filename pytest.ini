# Pytest configuration for this repository.
[pytest]
# Disable pytest's cache plugin so test runs do not create extra cache files.
addopts = -p no:cacheprovider
testpaths = tests
# Ignore generated output and temporary folders during test discovery.
norecursedirs = _work .tmp pytest-tmp .pytest-tmp pytest-cache .pytest_cache
